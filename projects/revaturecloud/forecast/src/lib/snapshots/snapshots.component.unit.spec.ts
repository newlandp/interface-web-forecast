import { SnapshotsComponent } from "./snapshots.component";

describe('forecast component', () => {
  let component: SnapshotsComponent;

  beforeEach(() => {
    component = new SnapshotsComponent();
  })

  it('exists', () => {
    expect(component).toBeTruthy();
  });
});
